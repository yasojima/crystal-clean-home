# 初めての方へ LP仕様

更新：2026-09-27。対象：`/first/`。完成済みの人物・漫画調・構成を維持する局所調整。

## 表現と背景

中央最大1040px、スマホ全幅でLPを完結する。外側の室内背景画像は撤去し、背面は無地 `#f4f5f5`。中央の青・水色・紺を軸に、生成り、セージ、琥珀、桃色を既存の家具・道具・服に取り入れる。強調文は控えめなオレンジ／深い緑を使う。Before／Afterとお客様の声は静かな背景とし、セクションの接続部は淡いグラデーションでなじませる。

既存の漫画の人物・会話・コマ割りは維持する。PCは相談6・作業7・仕上がり5場面、600px以下は縦長の相談6・作業6・仕上がり5場面。章番号は付けない。6つの姿勢と利用手順の番号は維持する。大見出しには不要な句読点を付けず、吹き出しと長い説明は自然な文章を保つ。

## 構成

冒頭画像＋赤い見積ボタン＋4項目ナビ → 相談の漫画 → 6つの姿勢 → 相談CTA → 作業の漫画 → 衛生管理 → 比較スライダー → 仕上がりの漫画 → お手入れCTA → 既存掲載文3件 → エアコン料金比較 → メニューCTA＋8カテゴリ → 利用・支払い5段階 → FAQ5問 → 悩みを振り返るチェック項目の画像 → シンク・浴室のBefore／After → メニュー選択・合計確認の案内 → 見積カートCTA → 先頭に戻る丸いボタン。

終盤の画像は同じアニメ調の人物を使い、文章を主役にする。「自分では落としきれない」「気になる場所がいくつもある」「忙しくて手が回らない」「まとめて頼むといくら？」の4項目を示す。漫画やCTAの同一画像を繰り返さない。中盤と終盤のBefore／Afterは設備と見せ方を変える。

## 画像と操作

- `brand/first-lp/art/` に14点。冒頭、3章、スマホ漫画3点、衛生、CTA4点、6つの姿勢、終盤の再訴求。背景画像は削除し、旧版はGitに保持。
- 内蔵image_genで局所編集／新規制作。採用画像の原PNG保存先と最終指示は `art-assets.json`。WebP変換は原寸、quality 94。意味のある画像の切り抜きは行わない。
- 冒頭画像の余分な空白を除き、見積ボタンと4項目ナビを直下の通常フローに置く。固定座標で大きな空白を確保しない。
- 赤いCTAは共有DOMを使い、リンクはボタンのみ。角丸8px、小さい線の矢印、共通の余白と文字比率にする。冒頭・中盤3箇所は `/services/`、最終「無料見積もりを試す」は `/cart/`。
- 残り4つのCTAはそれぞれ異なる既存場面の画像上に重ねる。スマホ幅でも最小44px以上の操作領域を確保する。
- 「漫画・画像の内容を文章で読む」の公開開閉欄は設けない。画像の説明はaltに保持し、会話原稿は `art-content.json` に内部保存する。
- LP末尾の丸い上矢印は `#first-introduction` へ移動。減速表示設定では即時移動。
- `/first/` のみ `.sec_cv` と共通footerを生成から外す。既存floating.jsはfooterのないページに追従バーを生成しない。他ページは変更せず、右上MENUは維持する。

## 既存部品の再利用

`materials.json` で中盤2組・終盤2組の比較画像、掲載文3件、FAQ5問を選択する。中盤は既存の `.c-tab`、`.c-compare-image` と共通JSのドラッグを使用し、LPの左右キー・Home・EndとARIA値の補完を維持する。ラベルはBefore／After。見出しは「こびりついた汚れも ここまできれいに！」、短い説明と注記は中央揃え。

終盤は既存まるごとページのシンクと水まわりページの浴室画像を、各2列の静止画で並べる。既存生成イメージであり、実際の施工写真とは扱わない短い注記を残す。

お客様の声はエアコン・水まわり・まるごとの既存本文・ロゴ・カードを抜粋する。既存正本は実顧客の口コミではないため、サンプル番号を除き、セクションに一つだけその事実を示す。内容を新たに創作したり、実口コミと断定しない。

8カテゴリは `brand/service-cards/template.html` のDOMと画像を再利用しPC4列／SP2列。フローは `flow-copy.json` と共通アイコンの5段階、FAQはホームの原稿と青いアコーディオンを使う。既存カートの価格・計算・予約機能は変更しない。

## 料金比較

壁掛けエアコンの通常・お掃除機能付き・室外機・防カビ抗菌コートを税込料金で比較する。淡い列色と当社欄の青い枠で読みやすくする。表だけ横スクロールし、ページ全体にはみ出させない。

表示は当社とA〜E。実社名と出典リンクの開閉欄、他社評価、最安・優位の断定は掲載しない。金額を創作せず、当社は現行カテゴリページの表示価格、A〜Eは通常料金。2026-09-26確認済みの公式情報・対応関係・条件は `comparison.json` に内部保持する。自社料金の最終採否は従来どおり事業者の確認事項。

## 正本と検証

構成 `template.html`、画像 `art/`、alt・会話原稿 `art-content.json`、比較 `comparison.json`、素材選択 `materials.json`、操作補完 `interaction.js`。配置は `source/device/{desktop,mobile}/css/first-lp.css`。

`first_lp.py` が既存部品のCSSをLPへスコープ化し、source/firstとdocs/firstを生成する。単独起動 `python -B first_lp.py` はshared_ui.pyまで実行する。再生成で共通フッター・追従バー・文章版や出典欄を復活させない。

`test_first_lp.py` で再生成の安定性、素材・本文・リンク、画像寸法、必要部分だけのCTA、終盤の順序とカート遷移先、別ページ不変を確認。`test_device_ui.py` はLPだけフッター非表示を許容する。非表示IABの表示・操作、公開確認、実機／人間検収は `SITE_VERIFICATION.md` で区別する。
