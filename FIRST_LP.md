# 初めての方へ LP仕様

更新：2026-09-26。対象：`/first/`。相談から納得、作業、仕上がりへと展開する画像主体のLP。

## 今回の承認内容

既に評価された人物・服装・漫画調・青系の上質な室内描写を維持する。章を均等な4コマに固定せず、5〜7場面に大小の強弱を付ける。PCは相談6・作業7・仕上がり5場面、600px以下は読みやすい縦長の相談6・作業6・仕上がり5場面。章番号は付けない。6つの姿勢と利用手順の番号は維持する。

LP全体の背面は、自然光の室内・植物・白い石材・青い曲線を描いた背景画像。中央最大1040px、スマホ全幅。最背面の補完色は `#e3f1fc`、紺 `#06408c`、青 `#096ba1`、白を使用する。ユーザーの [MILKY VEILメニューページ](https://yasojima.github.io/milky-veil-preview/menu/) のように外側まで絵が続く構成。

## 構成

一体の縦長ファーストビュー（タイトル・人物・見積ボタン・4つのページ内ボタン）→相談の漫画→6つの姿勢→相談場面のCTA→作業の漫画→衛生管理の長尺画像→既存の比較スライダー→仕上がりの漫画→お手入れ場面のCTA→既存掲載文3件→エアコン料金比較表→メニュー案内のCTAと既存8カテゴリ→実際の5段階利用・支払いフロー→ホームから抜粋したFAQ5問→玄関のお見送り場面の最終CTA→円形の上向きボタン。

画像間の長い説明と大きな白い余白を除き、短い大見出しを画像内へ統合。衛生管理の説明も画像内へまとめる。同じCTA画像を繰り返さず、それぞれ別の場面を制作する。

## 画像と操作

- `brand/first-lp/art/` に14点（今回新規13点＋既存quality1点）。冒頭、3章、スマホ漫画3点、衛生、CTA4点、背景、6つの姿勢。旧版はGitで保持。
- 内蔵image_genで制作。採用画像・原PNG保存先・最終生成指示は `art-assets.json`。WebP変換は原寸、quality 94。画像は切り抜かず表示する。
- 赤い無料お見積りボタンは共有CTAのDOM・リンク先を再利用し、画像に確保した余白へ重ねる。リンクはボタン部分だけ。現行の遷移先は `/services/`。
- 大きい画像内の文字と会話はaltと、読者が開ける静的HTMLの文章版に保存する。冒頭は優先読み込み、以下は遅延読み込み。
- 料金表は更新可能なHTML/CSSで背景画像と一体化。表だけ横スクロールし、ページ全体を横にはみ出させない。
- LP末尾の丸い上矢印は `#first-introduction` へ移動。減速表示設定では即時移動。
- `/first/` に限り `.sec_cv` と共通footerを生成から外す。既存floating.jsはfooterのないページでは追従バーを生成しないため、他ページの共通処理は変更しない。右上MENUは維持する。

## 既存部品の再利用

`materials.json` で清掃前後2組・掲載文3件・FAQの選択を管理する。既存商品の `.c-tab`、`.c-compare-image` と共通JSのドラッグ動作を再利用。LPでキーボードの左右・Home・EndとARIA値を補完し、清掃前／清掃後のラベルを常時表示する。画像は既存生成イメージと同一で、施工実績と誤認しない短い注記を残す。

お客様の声はエアコン・水まわり・まるごとの既存本文・ロゴ・カードをそのまま抜粋し、対応メニューへのリンクを付ける。既存正本は実顧客の口コミではないため、カードごとのサンプル番号は除き、セクションに1つだけその事実を示す。内容を新たに創作したり、実口コミだと断定しない。

8カテゴリは `brand/service-cards/template.html` のDOMと画像を再利用。PC4列／SP2列。フローは `flow-copy.json` と共通アイコンの5段階、FAQはホームの原稿から5問を抜粋し、既存の青いアコーディオンを使用する。

## 料金比較の根拠

対象は壁掛けエアコンの通常・お掃除機能付き・室外機・防カビ抗菌コート。自社は現在の本編の通常価格。A〜Eは実在5社の公式公開料金を2026-09-26に確認し、`comparison.json` に金額・出典・条件を保存する。架空の高い料金や口コミの悪評は作らない。最安と断定しない。

A=ダスキン、B=カジタク、C=クリーンクルー、D=おそうじ本舗、E=おそうじ革命。会社名と公式リンクは開閉式の出典欄で確認できる。キャンペーン／複数台割引を除外し、コートの内容差や室外機の設置条件を明記。カート価格は変更しない。自社料金の最終採否は従来どおり事業者の確認事項。

## 正本と検証

構成 `template.html`、画像 `art/`、文章版 `art-content.json`、比較 `comparison.json`、選択 `materials.json`、操作補完 `interaction.js`。端末別配置は `source/device/{desktop,mobile}/css/first-lp.css`。

`first_lp.py` が既存部品のCSSをLPへスコープ化し、source/firstとdocs/firstへ生成する。単独起動 `python -B first_lp.py` はshared_ui.pyまで実行。再生成は旧4コマ・共通フッター・追従バーを復活させない。

`test_first_lp.py` で再生成、既存本文・画像・リンクとの一致、ボタンだけのリンク、比較出典、別ページ不変を確認。`test_device_ui.py` はLPだけフッター非表示を許容する。非表示IABでPC/SP表示・実際のクリック／ドラッグ／キー操作を確認し、公開確認と実機／人間検収は `SITE_VERIFICATION.md` で区別する。
