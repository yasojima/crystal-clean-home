# ファーストビューの管理

2026-09-22 / CHG-2026-09-22-HERO

トップページの旧.heroバナーを、竹下設計のファーストビューの構造・CSS・スクロール処理へ差し替えた。右側のお見積もり・お問い合わせ・LINE導線、共通ヘッダー、本文は維持する。

## 正本

- brand/hero/desktop.css：PCフレーム。ヘッダー実測高さをstickyの停止位置に使用。高さは100dvhからヘッダー高を差し引く（100vhフォールバック）。後続20vh間隔、object-fit:coverでフレーム全面を表示。
- brand/hero/mobile.css：幅100%・画面高からヘッダー高を除いたフレーム、object-fit:cover。position:relativeの縦配置を保持。PCと分離して編集。
- brand/hero/script.js：参考のLenis duration:2/autoRaf:true、GSAP yPercent:0→0・scrub:trueの処理。見た目の重なりはCSSのstickyによるもの。GSAPで独自のズーム等を追加していない。
- brand/hero/slides.json：共通の画像・代替テキスト。採用した生成画像をソファ→エアコンフィルター→コンロの順に表示する。素材はbrand/hero/images/。
- brand/hero/vendor/：参照サイトから取得したLenis・GSAP・ScrollTrigger。ライセンス表記を保持。
- hero.py：トップページのみ生成。python build.py --hero-only、通常ビルドに対応。

## 移植元と組込み上の調整

参照：https://www.takeshita-sekkei.co.jp/
CSS：https://www.takeshita-sekkei.co.jp/css/style.css?0805 とstyle_sp.css?0805のMV節。
動作：トップページinline scriptのjs-imgScale処理、Lenis初期化。元ソースはローカルdesign/reference-hero/に保存。
参考の会社名・写真・実績説明・外部リンク・解析・別セクションは移植しない。移植CSSは#cch-heroに限定する。既存固定ヘッダーをResizeObserverで実測し、PCの上余白・停止位置・フレーム高さへ反映する（スマホ上余白0px）。端末境界は既存の993px/992pxに合わせる（参照元800px/799px）。スクロール補間はページ全体で継続し、ヘッダー・ダイアログ・メニューの操作を除外する。

## 初回移植時の確認（以下の改訂前）

1280×720で各フレーム高720px、後続間隔144px。スクロール720px時に先頭のtop:0と次のtop:294pxを確認。上下操作と右側導線の表示を確認。390pxでは横溢れなし・正方形の縦配置。JSエラーなし。トップページのヒーロー以外の本文は変更前と完全一致、生成処理の繰り返しでも差分なし。公開確認と画像選定は別の事項として記録する。


## CHG-2026-09-22-HERO-HEADER-OFFSET

PCの写真はヘッダー下端で固定し、その下で後続写真を重ねる。3枚目が同じ位置へ到達した後は、3枚が同時に上へ移動し、ヘッダー裏へ抜けて本文へ続く。フレーム高は画面高からヘッダー実測高を除いた値とする。写真・右側導線・スマホ側の既存表示は維持する。
確認：1280×720、ヘッダー150px、写真570px。scrollY720時の各写真上端は150/150/858px、1440時は138/138/138px、1692時は-114/-114/-114px。固定と同時解除をブラウザーの上下操作で確認。


## CHG-2026-09-22-HERO-FIT

ヘッダーと画像フレームを合わせてブラウザーの表示高に収める。PC・スマホとも100dvhからヘッダー実測高を差し引き、リサイズに追従する。画像は縦横比を維持し、object-fit:coverでフレーム全面を埋める。比率差によりはみ出す部分は中央基準で切り抜き、左右に白い余白を出さない。PCの3枚の重なり・一斉解除は維持する。
確認：非表示ブラウザー1280×720ではヘッダー150px＋フレーム570px、1440×1105では150px＋955px、390×844では60px＋784px。全サイズでフレーム下端が表示領域下端に一致、object-fit:contain、画像読込成功、横溢れなし。スマホ実機検収は別途。


## CHG-2026-09-22-SCROLL-HANDOFF

通常スクロールへ切り替える入力をheroのvirtualScrollで一括判定し、Lenis.reset()で進行中の慣性を停止、実スクロール位置へ同期してからブラウザーへ入力を渡す。ヘッダー・ダイアログ・見積もり・ヒーロー範囲外を対象とする。画像フレーム・CSSの重なり・3枚目到達後の一斉解除・既存メニューは維持。
ブラウザーで画像上の上スクロール直後にヘッダー上の下スクロールを実施。修正前は+135px直後に-153pxの逆戻り。修正後は1024→1168px（+144px）で停止し、逆戻りなし。


## CHG-2026-09-22-CONTINUOUS-SCROLL

高速ホイールでヒーロー範囲を越える際に慣性処理から通常処理へ切り替わり、移動量が41pxから2160pxへ急増する現象を確認。範囲判定を削除してページ全体のホイールを同じLenis処理へ統一した。ヘッダー・ダイアログ・見積もり内部への入力では前回のresetによる干渉防止を維持。画像フレームとstickyは無変更。
同じブラウザーで3ページ分の下入力を3回連続して比較。修正後の最大イベント移動量262px、逆方向への移動0回。参考サイトと同様に慣性を継続。これは境界の急変の修正確認であり、全端末の描画性能を保証しない。


## CHG-2026-09-22-HERO-PAINT

画像が重なる途中で断続的に引っかかるとの追加報告。終端の速度切替とは別の観点として、PCの3枚にwill-change:transform、画像フレームにcontain:paintを適用して描画領域を分離。配置・サイズ・停止位置は維持。1280×720でscrollY612のパネル上端150/252/966を確認。画像読込完了、各735×487でサイズ変動なし。性能低下の唯一の原因を特定したとは扱わず、ユーザー環境の断続症状解消は未確認。


## CHG-2026-09-22-HERO-FULL-BLEED

最新指示によりPC・スマホとも画像をcover表示へ変更。ヘッダー下の開始位置、フレーム高、スクロール処理、画像の順番、右側導線を維持する。従来の画像全体表示より余白なしの全面表示を優先する。
