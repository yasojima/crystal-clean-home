# 現在の検証状態

更新：2026-09-26。技術確認、公開確認、人間・実機の受け入れを区別する。過去の試作検証記録はGit履歴に保存する。

## 初めての方への仮統合

- 生成正本と公開用HTMLの本文一致、本文21段落・画像46要素の保持、3項目の順序、研修リンクの保持を確認。旧清掃ページと旧リンクは公開用HTMLに残らない。
- `test_device_ui.py` は58 CSS入口・441ページで通過。非表示IABのPCで清掃・感染予防の各アンカーへの表示と、ホームの4項目ナビから初めての方への遷移を確認。390px幅では3セクションの幅362px、横あふれ0px。コミット `2ca6ddc7` のPages run `36211866854` は成功。公開 `/first/` の本文が生成結果と一致し、3項目の順序と清掃アンカー、MENUの4項目を非表示IABで確認。 `/first/` と研修ページはHTTP 200、旧 `/reason/` は404。ホームもHTTP 200でナビ4項目。LP用本文は `source/first/lp-copy.md` に保存済み。公開本文の差し替え、LP構成・画像・CTAの制作は未実施。

## お問い合わせ見出し帯

- `shared_ui.py` の再生成と `test_device_ui.py`（58 CSS入口・442ページ）に通過。非表示IABのローカル `/contact/` で、上部H1の帯がPC幅で紺 `rgb(6,64,140)`・游明朝・高さ176.8px・全幅、390px幅で高さ141.1px・横あふれ0pxと確認。フォーム内H2と入力欄は表示を維持し、フォーム送信は行っていない。
- コミット `5d706f48` のPages run `36209495274` は成功。非表示IABの公開 `/contact/` で、PCの紺帯・游明朝・高さ176.8px・全幅とフォーム内H2を確認。公開ページを390px幅で再測定し、帯の高さ141.1px・横あふれ0pxを確認した。実機目視と実送信の検収は別とする。

## 公開済みの範囲

- GitHub Pagesは `main/docs` を配信。8商品ページ・ホーム・共通カート・見積を公開した。商品統合コミット `68ecab9e`（Pages run `36186846990`）、ホーム導線修正 `0054228d`（run `36188113518`）、資料同期 `ee452d33`（run `36188505380`）、法人ページ改修 `c8bd3509`（run `36203731390`）、共通フッター生成順の修正 `6d20af36`（run `36204513155`）はいずれも成功。
- ホーム、8商品ページ、カート、見積の計11 URLはHTTP 200で、生成済みHTMLと改行正規化後に一致。ホーム上下のカテゴリーカード計16件は対応する本編URLを指し、非表示IABで上部の洗濯機カードから公開 `/house-cleaning/washer/` への実遷移を確認した。
- 公開8商品ページを非表示IABのPC・390px幅で確認。各6件の掲載用サンプル口コミ、表示画像の読込失敗0件、390px幅の横あふれ0件。商品用アセット129件とカタログJSON/JS 2件はHTTP 200。全ページの人間による目視・実機検収を意味しない。

## 本編8ページと購入導線の技術確認

- `test_integrated_product_pages.py` で、8ページの本編DOMを生成入力と比較。商品カード297件、口コミ48件、比較画像20組、商品ID・バリエーション、画像とCSSの存在、ページ内アンカー、共通カタログ、ホーム上下16リンクを確認。生成済み本編に旧ラフURLが残らない。
- `test_device_ui.py` は端末別CSSの58入口と442ページの共通UIを確認。`test-reference.cjs`、`test-shop.cjs`、`test-checkout.cjs` は商品計算、カート保存、入力・確認のデモ動作で通過。実予約送信は行っていない。
- 非表示IABで本編「その他」の外壁手洗い洗浄を追加し、合計150,920円から194,920円（基本44,000円増）、追加1㎡で195,910円（990円増）へ変わることと、`/cart/` の明細・合計を確認した。
- オプション228件は個別の「詳しく見る」・専用ダイアログなし。親商品46件はデモ注記を押下時のダイアログに表示し、常時表示の注記なし。数量・追加・商品IDと価格を保持。320px幅で説明は最大3行、4行以上0件、横あふれ0件を確認した。
- 選択カードと下向き案内は8カテゴリーの対応するセクションへスクロールすることをPC・390px幅で確認。洗濯機の3種類も同一ページ内の個別見出しへ移動した。

## 未検収・未確定

- 実機での全ページ目視、事業者情報・サービス採否・料金・キャンペーン・支払い条件・受付先の最終承認は未実施。
- 実予約、決済、メール送信、全クーポン判定は未接続。掲載中の口コミと比較画像はデモ用であり、実在するお客様の声・施工実績ではない。
- 旧 `docs/_product-wireframe/` は生成経路で使用せず、現作業ツリーにも存在しない。旧画像等はローカルの `C:/Users/yasoj/Desktop/削除用/Crystal-Clean-Home_20260924/` に17点あり、人間による最終削除待ち。

## 法人向けページのローカル確認

- `corporation.py` の単独生成と `shared_ui.py` を実行。本文7箇所、掲載画像18点の差し替えと、旧画像URLの残存0件を生成HTMLで確認した。再生成後のフッター一致は `test_corporation.py` と `test_device_ui.py` で確認した。未参照のカテゴリー画像v1と重複した公開用の参考入力3ファイルは生成経路と参照を調べたうえで削除した。
- 非表示IABのローカル `/service/corporation/` で、帯の背景 `rgb(6,64,140)`、游明朝、幅1265pxを確認した。商品ページ側の実測176.8pxに合わせ、法人ページのCSS高さを176.8pxへ調整した。人物・埋め込み文字を含まない冒頭画像も画面上で確認した。
- 公開Pages run `36203731390` 成功後、非表示IABの公開ページで新本文、画像18点の参照、旧画像参照0件、PC帯の背景色・高さ176.8pxと画像の読込失敗0件を確認した。390px幅では帯の高さ141.1px、横あふれ0px、表示中のスマホ画像の読込成功を確認した。全画像の人間目視・スマホ実機での検収は未実施。
- 構図・視点を変えた新画像18点を `brand/corporation/images/` に置き、公開用 `docs/brand/corporation/images/` と18件すべてSHA-256一致を確認。`test_corporation.py` と `test_device_ui.py` は通過。非表示IABのローカル版で、法人見出し6箇所の左装飾が `none`、追加したセクション背景が透明、元のカード背景が `rgb(238,245,249)`、表示画像18点の読み込み成功を確認した。PC幅1821pxではカードが左右各568px、390pxでは横あふれ0pxで画像を表示した。コミット `ad6c1eea` のPages run `36206920302` 成功後、公開URLでも同じ見出し・背景の状態、画像18点の読み込み、公開画像とローカル画像のSHA-256全件一致、390px幅での横あふれ0pxとスマホ用画像の読込を確認した。実機・全画像の人間検収は未実施。
