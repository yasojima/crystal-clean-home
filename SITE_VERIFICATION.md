# Crystal Clean Home 公開・検証結果

検証日：2026-09-21

公開：https://yasojima.github.io/crystal-clean-home/
ソース：https://github.com/yasojima/crystal-clean-home
GitHub Pages：main /docs、公開処理成功。

## 取得と静的検証

- 取得成功：983ファイル（HTML 472件、その他511件）。サイトマップ403 URLから内部リンク・依存ファイルまで取得。
- 全472 HTMLでsection/main/header/footer/h1/h2/h3/img/formの要素数は取得元と一致。
- 変換後のローカルsrc/href/poster/data-src/data-lazy-src参照先の欠落：0件。
- 取得ソースのSHA256不一致：0件。
- 参照サイト側の404：5件。capture.jsonとverification.jsonにURLを記載。取得できなかったファイルを保存済みとは扱わない。

## ブラウザー・公開検証

- 非表示IABのみを使用。ユーザーのChromeと右側プレビューは使用していない。
- PCトップ：元サイト同様のヘッダー高さ150px。ロゴ・電話情報を反映。画像切れなし。
- スマホ390px：トップ・エアコンサービスページに横スクロールなし。トップ画像表示、メニュー開閉を確認。
- PC1440px：見積もり画面で通常タイプを0台から1台に変更し、合計0円から11,000円への更新を確認（参照元の仮料金）。
- 公開トップ・エアコンサービス・見積もりのHTTP 200と、主要ロゴ・写真のHTTP 200を確認。HTMLはGitによる改行正規化を除いて一致。画像はバイト一致。
- 公開トップのタイトル・ロゴ・読み込み済み画像にエラーなし。

## 限界と残件

全472ページを目視したものではない。取得できるのは公開HTML/CSS/JS/画像等で、WordPressのPHP・DB・メール送信など非公開ソースは含まれない。送信先未設定のフォームは送信を止める。画像に焼き込まれた参照元の名称・実績等はそのまま残る。事業情報は未確定。


外部AI監査・PDCAはオーナー指示により適用除外。通常の自己検証結果と区別する。

## 2026-09-22 要件更新の状態

CHG-2026-09-22-BRAND：ユーザーが書体見本を確認し、用途別の游明朝・游ゴシックを採用。配色・コンセプト・動きとあわせて要件定義書v1.1と関連MDへ反映。今回ソース・公開ファイルは変更していない。上記の既存検証結果は新デザインの適用・検証を意味しない。

ローカル資料確認：要件定義書と関連MDの現行方針を照合。採用書体見本をプロジェクト内design/へ保存し、元画像とのSHA256一致を確認。Driveへの書き込みなし。

## 2026-09-22 書体・配色の実装確認

非表示IABでトップとエアコンサービスをPC・390px幅で確認。大見出しはYu Mincho、本文はYu Gothic。スマホで横方向のはみ出しなし。CSSの配置・寸法・余白・文字サイズ・行高に変更なし、SVGのviewBoxとpath形状に変更なし。HTML・写真・採用ロゴに差分なし。全ページの目視確認ではない。

## CHG-2026-09-22-COPY-DEMO 文章と接続先

- サイト本体387ページへ文章置換を適用。延べ37,604箇所、変更前後1,835組を文章変更一覧.mdに記録。料金・事実・引用・画像文字等の維持範囲も同書に記載。
- 通常の案内文へデモ注意書きを混ぜた実装を修正。電話・LINE・外部接続・最終送信の操作時のみ中央ポップアップで説明する。
- 全387ページの外部リンク、8フォームの送信先を解除。入力確認とブラウザ内の見積計算を維持。
- 公開コミット：5e28374182f03bab776bd4a9c15956817d1d7308。Pages実行35664113073成功。公開4ページと共通JS/CSSのHTTP 200・ローカル一致を確認（改行正規化）。
- 非表示IABで電話・LINE・入力確認から最終送信・Esc・390px幅の表示を確認。全ページの目視検証ではない。
- 要件定義書v1.2と関連MDを指定のローカル内で更新。Driveの作成・編集・同期は実施していない。

## CHG-2026-09-22-VIEWPORT-HUD

全387ページ右下に黒い小型ラベルで「幅 × 高さ px · DESKTOP／TABLET／MOBILE」を常時表示する。window.innerWidth/innerHeight（CSS px）を使用し、リサイズ時に即時更新する。既存主要メディアクエリに合わせMOBILEは640px以下、TABLETは641～992px、DESKTOPは993px以上。前面固定で、クリックを遮らない。ダイアログ表示中も前面を維持する。印刷には出さない。
編集正本はbrand/viewport-hud.js、共通組込みはhud.py。通常ビルドとpython build.py --hud-onlyで反映する。
非表示IABで390・640・641・992・993・1280pxの表示と高さ変更、ダイアログ表示時を確認。全387ページに1回だけ読み込まれ、HUDの読込タグ以外のHTML本文が変更されていないことを照合した。

HUD公開確認：コミット7db8bb70fb9a46cdcaeeebc9ed4777ca56d938fd、Pages実行35666357171成功。公開トップで1280 × 720 px · DESKTOPの表示を確認。


共通ヘッダー公開確認：コミットe1eca7dce90dd0609f287dc2746255d3473ce0c6、Pages実行35668221857成功。公開トップ・下層ページ・共通CSS/JSがローカルと一致（改行正規化）。公開ブラウザでメニュー展開を確認。操作・内容保持の検証はHEADER_UI.mdおよびdesign/header-verification.jsonを参照。


2026-09-22：ホーム追加・パンくず文字撤去はc34763a、Pages 35670130130成功。端末別CSS管理はded3811、Pages 35670654995成功。公開トップ・下層・PC/スマホヘッダーCSS・本体CSSとローカル生成物の一致を確認。PC1280pxで帯31.96875px・本文開始245.96875pxは変更前と同じ。390pxのホーム移動・メニュー表示と既存詳細21リンクを確認。サイト/見積CSSは390/640/768/992/993/1280/1440pxで有効ルールの順序を含め分離前と一致。全387ページの目視確認ではない。


2026-09-22 HERO：6c585bc、Pages 35679374899成功。公開HTML・PC/スマホCSS・動作JSの生成物一致を確認。画像は仮置き、ユーザーの動作確認待ち。


## 2026-09-22 HERO-FIT 公開確認
Commit: 1c777078ddb172aa57fc917d6dae154e8a7e1e5c。Pages run 35680240515 success。公開トップURL・brand/hero/desktop.css・mobile.css・script.jsが改行コード正規化後にローカル生成物と一致。PC 1280×720/1440×1105、スマホ幅390×844のブラウザー検証はHERO_UI.md参照。実機・全ブラウザーの検収を意味しない。


## SCROLL-HANDOFF 公開確認
2026-09-22: f648ef47e54269795536f5204ff5af01da80a427。Pages 35681509448 success。公開トップ・hero/script.jsの改行正規化後一致を確認。非表示ブラウザーで修正前のヘッダー切替時の逆戻りが解消。本文境界の往復、画像の重なり・全3枚一斉解除、JSエラーなしを確認。


2026-09-22 c14cd29: Pages35684690659成功。公開トップ・hero/desktop.css・hero/script.jsが生成物と改行正規化後一致。描画調整の断続的症状への効果はユーザー環境で未確認。
