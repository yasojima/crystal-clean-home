(() => {
'use strict';
const BASE='/crystal-clean-home/', data=JSON.parse(document.getElementById('shop-catalog').textContent), map=CCHCart.index(data), key='cch-estimate-cart-v1', app=document.getElementById('shop-app'), page=document.body.dataset.shopPage;
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const yen=n=>n.toLocaleString('ja-JP')+'円';
let cart=[];try{cart=CCHCart.clean(JSON.parse(localStorage.getItem(key)||'[]'),map)}catch{}
function save(){try{localStorage.setItem(key,JSON.stringify(cart))}catch{document.getElementById('shop-status').textContent='ブラウザーの保存機能が無効です。カートを次の画面へ引き継げません。保存を許可してご利用ください。'}dock()}
function dock(){const t=CCHCart.totals(cart,map);document.getElementById('cart-count').textContent=t.lines;document.getElementById('cart-total').textContent=yen(t.total)+(t.quote?'＋個別見積り':'');document.querySelector('.shop-cart-dock').hidden=page!=='services'}
function progress(step){return `<ol class="shop-progress">${['サービスを選ぶ','内容・合計を確認','見積もり予約'].map((v,i)=>`<li ${i===step?'aria-current="step"':''}><span>0${i+1}</span>${v}</li>`).join('')}</ol>`}
function price(v){return v.price===null?'<strong>個別見積り</strong>':`<strong>${yen(v.price)}</strong><small>（税込）／${esc(v.unit)}</small>`}
function card(p){const v=p.variants[0];return `<article class="shop-product" data-product="${p.id}"><div class="shop-product-images ${p.images.length>1?'is-pack':''}">${p.images.map(src=>`<img src="${src}" alt="${esc(p.name)}の清掃イメージ" loading="lazy" width="480" height="280">`).join('')}</div><div class="shop-product-body"><p class="shop-category-label">${esc(data.categories.find(c=>c[0]===p.category)[1])}</p><h3>${esc(p.name)}</h3><p class="shop-description">${esc(p.description)}</p>${p.detail?`<a class="shop-detail" href="${p.detail}" target="_blank" rel="noopener">清掃範囲・詳細を見る ↗</a>`:'<p class="shop-detail-note">対応範囲はご予約時に確認します。</p>'}<label class="shop-variant-label">種類・プラン<select data-variant aria-label="${esc(p.name)}の種類">${p.variants.map(v=>`<option value="${v.id}">${esc(v.name)}</option>`).join('')}</select></label><p class="shop-price">${price(v)}</p><div class="shop-product-action"><label><span data-unit>数量（${esc(v.unit)}）</span><input type="number" min="1" max="${v.max}" step="1" value="1" data-qty aria-label="${esc(p.name)}の数量"></label><button class="shop-button" data-add>カートに追加 <span>＋</span></button></div></div></article>`}
function catalog(){const requested=new URLSearchParams(location.search).get('category');const category=data.categories.some(c=>c[0]===requested)?requested:'all';app.innerHTML=`<section class="shop-intro"><div class="shop-wrap"><p class="shop-eyebrow">HOUSE CLEANING</p><h1>きれいにしたい場所から、<br>サービスを選ぶ。</h1><p>清掃内容と料金を比べて、気になるサービスをカートへ。<br>ご希望をまとめて、お見積もりをご相談いただけます。</p>${progress(0)}</div></section><section class="shop-catalog shop-wrap" aria-label="サービス一覧"><div class="shop-section-title"><div><p class="shop-eyebrow">SERVICE MENU</p><h2>清掃メニューと料金</h2></div><p>表示価格は税込です。<br>設置状況や作業範囲により正式なお見積もりをご案内します。</p></div><div class="shop-filters" role="group" aria-label="清掃カテゴリー">${data.categories.map(c=>`<button type="button" data-filter="${c[0]}" aria-pressed="${c[0]===category}">${c[1]}</button>`).join('')}</div><p id="result-count" role="status"></p><div class="shop-grid" id="products"></div></section><section class="shop-help"><div class="shop-wrap"><p class="shop-eyebrow">HOW TO REQUEST</p><h2>ご相談まで、かんたん3ステップ。</h2><div class="shop-how"><div><b>01</b><h3>清掃メニューを選ぶ</h3><p>種類と数量を選択してカートに追加。複数の場所をまとめて選べます。</p></div><div><b>02</b><h3>内容と合計を確認する</h3><p>数量の変更や削除もカートで。気になる箇所を整理できます。</p></div><div><b>03</b><h3>見積もりを相談する</h3><p>連絡先とご希望を入力。作業の内容や日程は、スタッフと確認します。</p></div></div><details><summary>カートに入れると注文が確定しますか？</summary><p>カートへの追加だけでは確定しません。内容をご確認いただき、見積もり予約へお進みください。</p></details><details><summary>機種や清掃範囲がわからない場合は？</summary><p>近いメニューを選び、ご予約時の相談欄にご記入ください。対応の可否や正式な料金は確認のうえご案内します。</p></details></div></section>`;filter(category)}
function filter(cat){const items=data.products.filter(p=>cat==='all'||p.category===cat);document.getElementById('products').innerHTML=items.map(card).join('');document.getElementById('result-count').textContent=`${items.length}件のメニュー`;app.querySelectorAll('[data-filter]').forEach(b=>b.setAttribute('aria-pressed',String(b.dataset.filter===cat)));const u=new URL(location);cat==='all'?u.searchParams.delete('category'):u.searchParams.set('category',cat);history.replaceState(null,'',u)}
function summary(){const t=CCHCart.totals(cart,map);return `<aside class="shop-summary"><p>お見積もり内容 <b>${t.lines}項目</b></p><div class="shop-summary-total"><span>概算合計（税込）</span><strong>${yen(t.total)}</strong></div>${t.quote?'<p>別途、個別見積りのご相談を含みます。</p>':''}<p class="shop-muted">正式な料金は、機種・作業範囲・現地の状況を確認してご案内します。</p>${page==='cart'?`<a class="shop-button" href="${BASE}estimate/">見積もり予約へ進む →</a><a class="shop-continue" href="${BASE}services/">サービスを追加する</a>`:''}</aside>`}
function empty(){return `<div class="shop-empty"><h2>カートは空です</h2><p>気になる清掃サービスをお選びください。</p><a class="shop-button" href="${BASE}services/">サービス一覧を見る →</a></div>`}
function cartPage(){app.innerHTML=`<div class="shop-wrap shop-page"><p class="shop-eyebrow">YOUR ESTIMATE</p><h1>お見積もり内容</h1>${progress(1)}${!cart.length?empty():`<div class="shop-checkout"><div class="shop-lines">${cart.map(l=>{const v=map.get(l.id);return `<article class="shop-line" data-line="${l.id}"><img src="${v.product.images[0]}" alt="${esc(v.product.name)}" width="160" height="112"><div><h2>${esc(v.product.name)}</h2><p>${esc(v.name)}</p><p>${v.price===null?'個別見積り':yen(v.price)+'（税込）／'+esc(v.unit)}</p><label>数量（${esc(v.unit)}） <input data-line-qty type="number" min="1" max="${v.max}" step="1" value="${l.qty}" aria-label="${esc(v.product.name+' '+v.name)}の数量"></label><button class="shop-remove" data-remove>削除</button></div><strong class="shop-line-total">${v.price===null?'個別見積り':yen(v.price*l.qty)}</strong></article>`}).join('')}</div>${summary()}</div>`}</div>`}
function estimate(){app.innerHTML=`<div class="shop-wrap shop-page"><p class="shop-eyebrow">REQUEST AN ESTIMATE</p><h1>見積もりのご予約</h1>${progress(2)}${!cart.length?empty():`<div class="shop-checkout"><div><form id="estimate-form"><p class="shop-form-lead">ご連絡先とご希望をお聞かせください。<br>この時点で作業日・金額は確定しません。</p><div class="shop-form-grid"><label>お名前 <em>必須</em><input name="name" autocomplete="name" required maxlength="80" placeholder="例：山田 太郎"></label><label>フリガナ<input name="kana" maxlength="80" placeholder="例：ヤマダ タロウ"></label><label>電話番号 <em>必須</em><input name="phone" type="tel" autocomplete="tel" required maxlength="20" placeholder="例：090-1234-5678"></label><label>メールアドレス <em>必須</em><input name="email" type="email" autocomplete="email" required maxlength="160" placeholder="例：name@example.com"></label></div><label>訪問先の住所 <em>必須</em><input name="address" autocomplete="street-address" required maxlength="200" placeholder="都道府県・市区町村・番地・建物名"></label><div class="shop-form-grid"><label>ご連絡の希望日<input name="date" type="date" min="${localDate()}"></label><label>ご希望の連絡方法<select name="method"><option>電話</option><option>メール</option><option>どちらでも可</option></select></label></div><label>ご相談内容<textarea name="message" rows="5" maxlength="2000" placeholder="機種、気になる汚れ、ご都合のよい時間帯などをご記入ください。"></textarea></label><p class="shop-muted">日程や対応地域を確認のうえ、ご案内するためのご予約です。</p><button class="shop-button" type="submit">入力内容を確認する →</button><a class="shop-continue" href="${BASE}cart/">カートに戻る</a></form><section id="estimate-review" hidden tabindex="-1"><h2>ご予約内容の確認</h2><dl id="review-fields"></dl><h3>お見積もり内容</h3><ul class="shop-review-items">${cart.map(l=>{const v=map.get(l.id);return `<li>${esc(v.product.name)} ／ ${esc(v.name)} × ${l.qty}${esc(v.unit)}</li>`}).join('')}</ul><div class="shop-dialog-actions"><button class="shop-button" type="button" id="finish">この内容で見積もりを予約する</button><button class="shop-secondary" type="button" id="edit-form">入力内容を修正する</button></div></section></div>${summary()}</div>`}</div>`;const form=document.getElementById('estimate-form');if(form)form.addEventListener('submit',e=>{e.preventDefault();const phone=form.elements.phone;phone.setCustomValidity(/^[0-9+() ー－-]{10,20}$/u.test(phone.value)&&phone.value.replace(/[^0-9]/g,'').length>=10?'':'電話番号を10桁以上の数字で入力してください。');if(!form.reportValidity())return;const fields=new FormData(form);const labels={name:'お名前',kana:'フリガナ',phone:'電話番号',email:'メールアドレス',address:'訪問先の住所',date:'ご連絡の希望日',method:'ご希望の連絡方法',message:'ご相談内容'};document.getElementById('review-fields').innerHTML=[...fields].map(([k,v])=>`<dt>${labels[k]}</dt><dd>${esc(v||'指定なし')}</dd>`).join('');form.hidden=true;const r=document.getElementById('estimate-review');r.hidden=false;r.focus();r.scrollIntoView({block:'start'});})}
function localDate(){const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}
let returnFocus;
function showDialog(id){returnFocus=document.activeElement;document.getElementById(id).showModal()}
document.querySelectorAll('dialog').forEach(d=>{d.addEventListener('close',()=>returnFocus?.focus());d.querySelector('[data-close]').addEventListener('click',()=>d.close())});
app.addEventListener('change',e=>{if(e.target.matches('[data-variant]')){const article=e.target.closest('[data-product]'),v=map.get(e.target.value);article.querySelector('.shop-price').innerHTML=price(v);article.querySelector('[data-unit]').textContent=`数量（${v.unit}）`;article.querySelector('[data-qty]').max=v.max;article.querySelector('[data-qty]').value=1}if(e.target.matches('[data-line-qty]')&&!e.target.validity.valid){const id=e.target.closest('[data-line]').dataset.line;e.target.value=cart.find(l=>l.id===id).qty;document.getElementById('shop-status').textContent='数量は指定範囲の整数で入力してください。'}});
app.addEventListener('input',e=>{if(e.target.name==='phone')e.target.setCustomValidity('');if(!e.target.matches('[data-line-qty]')||!e.target.validity.valid)return;const line=e.target.closest('[data-line]'),id=line.dataset.line,v=map.get(id);cart=cart.map(l=>l.id===id?{...l,qty:Number(e.target.value)}:l);save();line.querySelector('.shop-line-total').textContent=v.price===null?'個別見積り':yen(v.price*Number(e.target.value));app.querySelector('.shop-summary').outerHTML=summary();});
app.addEventListener('click',e=>{const filterButton=e.target.closest('[data-filter]');if(filterButton)filter(filterButton.dataset.filter);if(e.target.closest('[data-add]')){const article=e.target.closest('[data-product]'),input=article.querySelector('[data-qty]');if(!input.reportValidity())return;const id=article.querySelector('[data-variant]').value,v=map.get(id),qty=Number(input.value),existing=cart.find(l=>l.id===id);if((existing?.qty||0)+qty>v.max){document.getElementById('shop-status').textContent=`このメニューは最大${v.max}${v.unit}まで選べます。`;return}if(existing)existing.qty+=qty;else cart.push({id,qty});save();document.getElementById('added-name').textContent=`${v.product.name} ／ ${v.name} × ${qty}${v.unit}`;showDialog('added-dialog')}if(e.target.closest('[data-remove]')){const id=e.target.closest('[data-line]').dataset.line;cart=cart.filter(l=>l.id!==id);save();cartPage();app.querySelector('[data-line-qty],.shop-empty a')?.focus();document.getElementById('shop-status').textContent='カートから削除しました。'}if(e.target.id==='edit-form'){document.getElementById('estimate-review').hidden=true;document.getElementById('estimate-form').hidden=false;document.querySelector('[name="name"]').focus()}if(e.target.id==='finish')showDialog('demo-dialog')});
window.addEventListener('storage',e=>{if(e.key!==key)return;try{cart=CCHCart.clean(JSON.parse(e.newValue||'[]'),map)}catch{cart=[]}dock();if(page==='cart')cartPage();if(page==='estimate'){estimate();document.getElementById('shop-status').textContent='別の画面でカートが変更されたため、予約内容を更新しました。'}});
document.querySelectorAll('[data-demo-action="phone"]').forEach(a=>{a.addEventListener('click',()=>showDialog('phone-dialog'));a.addEventListener('keydown',e=>{if(['Enter',' '].includes(e.key)){e.preventDefault();a.click()}})});
window.addEventListener('pageshow',e=>{if(!e.persisted)return;let next=[];try{next=CCHCart.clean(JSON.parse(localStorage.getItem(key)||'[]'),map)}catch{}if(JSON.stringify(next)===JSON.stringify(cart))return;cart=next;dock();if(page==='cart')cartPage();if(page==='estimate')estimate();});
if(page==='services')catalog();else if(page==='cart')cartPage();else estimate();dock();
})();
