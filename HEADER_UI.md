# 共通ヘッダーの管理

更新日：2026-09-22 / CHG-2026-09-22-SHARED-HEADER

## 編集する場所

全387ページのヘッダーは `brand/header/` に集約しています。各ページのHTMLを個別に修正しません。

| ファイル | 管理するもの |
| --- | --- |
| brand/header/template.html | ロゴ・連絡先の枠、グローバルメニュー名、見出し、詳細項目、リンク先 |
| brand/header/style.css | 共通のすりガラス・配色・装飾帯 |
| brand/header/desktop.css | PCの配置・余白・メガメニューの表示 |
| brand/header/mobile.css | スマホ側の配置・余白・一覧の表示 |
| brand/header/script.js | ホバー・タップ・キーボードによる開閉、スマホのスクロール制御 |
| brand/header/pages.json | ページ固有の追加CSS・JS。現在は空で、全ページ共通 |
| brand/site.json | 既存どおり、店名・電話番号・受付時間の情報 |
| header.py | 共通テンプレート・共通アセット・ページ別追加を公開HTMLへ組み込む処理 |

`python build.py --header-only` で共通部品を全ページへ反映します。通常の `python build.py` にも組み込まれています。トップページのロゴ見出しはh1、下層ページはdivとして出力します。

## 保持する情報

ホームを加えたグローバルメニュー7項目、サービス内の詳細21リンク、分類見出し、連絡先の導線を維持します。現在サブメニューがあるのは「サービスと料金」のみです。他の5項目に新しい詳細項目を作り足さず、元のリンク先へ移動します。電話のデモ案内とViewport HUDも維持します。

## 動きと外観

ヘッダーは上部に固定し、白20%の半透明背景と背面のぼかしを使用します。ロゴ・文字はぼかしません。旧実装の「下スクロールでロゴと電話の行を隠す」挙動は廃止し、参考サイトと同じくヘッダーを上部に保ちます。

PCではホバーで白い横長のメガメニューを展開します。フェードと縦方向10pxの移動を0.4秒で行います。ヘッダー外、別の通常項目、ロゴ欄へカーソルを移すと閉じます。詳細リンクの下線は左から伸びます。参考元の黄色は既存の青に合わせています。ロゴ・電話・メニュー文字の既存書体は維持します。

992px以下では既存のメニューボタンからタップで開き、白い一覧内で詳細を選択します。開いている間は背景のスクロールを抑制し、右上の閉じるボタンから閉じられます。Enter・Space・矢印下・Esc・Tabに対応します。動きを減らす端末設定ではアニメーションを省略します。

## ページ固有の追加機能

共通テンプレートをコピーせず、`pages.json` に対象の出力パスをキーとして `styles` と `scripts` を指定します。値は `brand/header/` 内の相対ファイルパスです。指定されたページだけ、共通部品の後に追加アセットを読み込みます。通常の共通修正は全ページへ引き続き適用されます。

設定例（現在の設定には追加していません）：

```json
{
  "contact/index.html": {
    "styles": ["extensions/contact.css"],
    "scripts": ["extensions/contact.js"]
  }
}
```

## 参照ソースと適合

- [参考サイト](https://idhome.co.jp/)
- [ヘッダーCSSを含むcommon.css](https://idhome.co.jp/cms/wp-content/themes/idhome_theme/css/common.css)：`.l-header`、`.l-header__mega`、`.l-header__mega-list`、`.l-header__mega-link` の背景・ぼかし・パネル形状・3列配置・下線・transitionを既存のDOMへ移植。
- [ヘッダーJSを含むcommon.js](https://idhome.co.jp/cms/wp-content/themes/idhome_theme/js/common.js)：メガメニューのmouseenter、ヘッダーのmouseleave、通常項目・ロゴへの移動で閉じる処理を、既存メニュー向けに適合。
- 時間0.4秒、移動10px、transformのcubic-bezier(.43,.05,.17,1)、materialのcubic-bezier(.26,.16,.1,1)は参照ソースの値を使用。参考サイトの社名・メニュー項目・リンク・解析・フォーム処理は移植しません。

取得した比較用ソースはローカルの `design/reference-header/` に保存しています。実行時には自社の共通部品だけを読み込みます。

## 確認結果

全387ページでメニュー名・詳細項目・リンク先が変更前の一覧と一致。ヘッダー外の本文が変わっていないことも照合しました。検証記録は `design/header-verification.json`。非表示IABでPCのホバー開閉、背景ぼかし、下層ページ、993px境界、390pxのタップ開閉、詳細21項目、キーボード、電話のデモ案内を確認しました。全ページの目視検証ではありません。


## CHG-2026-09-22-HOME-NAV

[変更 2026-09-22]
グローバルメニューの一番左に「HOME」を追加し、トップページへ移動する。ホームから会社概要まで7項目をbrand/header/template.htmlで一括管理する。既存6項目と詳細21リンクは維持する。書体・文字サイズ・上下余白は既存の共通規則を使い、項目間は既存の均等な空きの配置を維持する。各項目の先頭一文字は共通の青で統一する。
パンくずの文字とリンクは全ページから撤去する。既存のグレーの帯・高さ・本文位置は維持し、空の装飾帯として支援技術から除外する。共通生成処理はheader.py、外観はbrand/header/style.cssで管理する。Driveは変更しない。

端末別編集の詳細はDEVICE_LAYOUT.mdを参照。
