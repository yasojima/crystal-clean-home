# 共通UI仕様

更新：2026-09-23。

ヘッダー内容：brand/header/template.html、事業情報：brand/site.json。HOME／サービスと料金／清掃への取り組み／初めての方へ／ご利用前のご質問／対応地域。会社概要はヘッダーに表示しない。
ヘッダーの表示はホームのみ。全幅のメディア上へ重ね、スクロールで上へ抜ける。カード案内は電話左、VisaとMastercard、黒帯・白文字「対応カード」。詳細メニュー面は白、ホバー横線は黄色。

右上MENUはbrand/header/floating.jsが同じメニュー内容から生成。下層ページは常設、PC上端8px・SP上端0px。ホームPCはヘッダー通過後に表示。開閉・アコーディオンは移植済みの動作を保持。スクロールバー幅を予約し横ずれを防ぐ。

追従バーは同じfloating.jsで生成。スクロール中は退避、停止後500msで復帰、フッター直上で停止。左はカートアイコンとお見積り、右はお問い合わせ。両方の文字は同じ書体。電話下は黒文字で「※営業電話は業務に支障をきたす為、ご遠慮ください。」。
カート概要は現在点数・合計を表示し/cart/へ接続。外側クリック／タップとEscapeで閉じる。旧ページの右側問い合わせ・LINE・シミュレーションは表示しない。

フッター文章・リンクはbrand/shared-ui/footer.htmlに集約。shared_ui.pyが全UIページへ配布し、PC/SPの表示は各端末CSSへ分離。ロゴ・支払方法・電話段は撤去済み。

見た目の正本はsource/device/{desktop,mobile}/css/brand/header/とcss/brand/shared-ui/。画像選択とUI寸法は同階層のimages.json、ui.json。入口CSSは生成用振り分けファイルのため直接編集しない。意味上のDOMと文言・カート状態は共有。


## メニュー・追従バーの現行調整

メニューは画面高に追従し、6項目の間隔を残り高さへ配分する。サービス展開時と極端に低い画面では内部スクロールを許可する。開閉アニメーションは維持し、開いている間は下部追従バーを下へ退避させる。下部に電話・受付時間・お問い合わせ・カートへのリンク・SNS4種を表示。SNSのアカウントURLは未設定で、仮リンクは作らない。

追従バーのお見積りは背景なし、アイコンと文字は赤 #ed0e42。書体はお問い合わせと同じ。受付時間の右隣にVISA・Mastercardのアイコンのみ配置する。追従バーでは「対応カード」ラベルを表示せず、PCは48×32px、スマホは36×24pxで横並びにする。フッターの折り畳み見出しは白背景・黒文字、hover時は青背景・白文字。

グローバルナビを黄色にする案はキャンセル。通常のホバー色は維持し、ドロップダウンの動く横線を #26a9e8 に変更。ヘッダーの電話アイコンは青の塗りつぶし。

メニュー下部CTAは左がお見積り（赤背景・白文字）、右がお問い合わせ。主項目はヘッダーと同じ游ゴシック700、通常#4d4d4d、頭文字#075b91、hoverで全体を#075b91へ変化させる。
